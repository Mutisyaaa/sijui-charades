# Optional Sijui Charades sounds

Drop these files into this folder to replace the built-in synthesized sounds:

- `right.mp3` — correct answer
- `wrong.mp3` — skipped answer
- `end.mp3` — round finished

The game falls back to its built-in sounds if a file is missing. 🎵
